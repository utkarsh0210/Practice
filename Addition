/*Goal: Practice writing generic functions*/

#include<iostream>


template <typename T> 
T sumTwo(T a,T b)
{
    T  sum;
    sum = a + b;
    return sum;
}
int main()
{
    int a = 90;
    int b = 1; 
    std::cout<<a <<" + "<<b<<" = "<< sumTwo(a,b);
    return 0;
}
