#include <stdio.h>

int main(void) {
  int t, n, x, y, z, count, i, j, a, b, c;
  scanf("%d", &t);
  while (t--) {
    scanf("%d %d %d %d", &a, &b, &x, &y);
    if ((b * b) >= (a * a) - (2 * x * y))
      printf("Yes\n");
    else
      printf("No\n");
  }
  return 0;
}
