#include <stdio.h>

int main(void) {
  int t;
	float x,y,i,a,b;
	scanf("%d",&t);
	while(t--)
	{
	  scanf("%f %f %f %f",&x,&a,&b,&y);
    i=x+(x*y)/100;
	  if(i>=a && i<=b)
      printf("Yes\n");
    else
      printf("No\n");
	}
	return 0;
}
