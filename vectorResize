#include<iostream>
#include<vector>
int main ()
{
  //TODO: instantiate vFloat vector
  std::vector<int> vFloat;
  std::cout<<"vFloat has "<<vFloat.size()<<" elements\n";
  
  
  //TODO: Change the size of vFloat to 10 elements
  vFloat.resize(10);
  
  std::cout<<"\n\n vFloat now has "<<vFloat.size()<<" elements\n";
 
  return 0;
}
